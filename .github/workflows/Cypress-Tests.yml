name: Cypress tests
on: [push]
jobs:
  cypress-run:
    name: Cypress run
    runs-on: ubuntu-20.04
    steps:
      - name: Checkout
        uses: actions/checkout@v2
        
      - name: opening-localhost
        run: npx @11ty/eleventy --serve 
             npm test
        
      - name: Cypress run
        uses: cypress-io/github-action@v2
        with:
          spec: |
            cypress/integration/examples/axe.spec.js
